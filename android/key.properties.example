# Keystore for release signing - copy to key.properties and fill real values
# This file is gitignored - never commit real keys
storeFile=../keystores/greetings-release.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=greetings
keyPassword=YOUR_KEY_PASSWORD
